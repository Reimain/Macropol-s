{
  "name": "@slpie/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Read-and-approve, not full administration. Shares the generated client and the query hooks with the web console.",
  "scripts": {
    "contract": "slpie contract --typescript > src/slpie-client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-native": "0.75.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
